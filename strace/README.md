# strace

## Description

A simple implementation of strace using the ptrace system call.

The program executes a command and prints the syscall number each time a system call is intercepted.

## Compilation

```bash
make strace_0
